doctrine:
  dbal:
    # On définit l'utilisation de sqLite
    driver: pdo_sqlite
    # On définit le chemin vers la base de données
    path: "%kernel.cache_dir%/test.db"
    # On stipule que le fichier n'est pas sur une url distante
    url: null
